# Ubo - Github Runner Resources

- To set up a runner, run `scripts/setup.sh`.
- To run a runner, run `scripts/run.sh`.

## Future plans

We will have a image for this purpose which automatically runs the runner after boot
